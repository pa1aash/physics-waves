# data/raw/

Immutable primary simulation output, one subdirectory per run ID (never by date
or description). HDF5 field snapshots, conservation series, spectra and Hovmöller
slices land here at runtime. Contents are gitignored and never committed. Nothing
ever writes back into this directory after a run completes.
