# src/figures/

Figure production. `style.py` (implemented) defines the house matplotlib style,
Springer-sized figures, colour-blind-safe colormaps and the provenance-sidecar
helper. `make_figures.py` (stub until Session L6) builds the final figure set
from aggregated results.
