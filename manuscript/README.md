# manuscript/

Springer Nature manuscript sources. `template/` receives the downloaded Springer
Nature LaTeX bundle (supplied by the operator, not vendored). All submission
files sit in a single flat directory, with the `.bbl` pre-compiled and submitted
alongside the `.tex`. Drafted in the final project phase.
