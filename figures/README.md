# figures/

Final publication figure set. Each figure is accompanied by a sidecar JSON
recording the run IDs, processed-data files, config hashes and git commit it was
built from. Vector PDFs are tracked via Git LFS. Built by
`src/figures/make_figures.py` at Phase 10; empty until then.
